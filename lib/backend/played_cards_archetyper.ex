defmodule Backend.PlayedCardsArchetyper do
  @moduledoc false

  def archetype(cards, class) when is_binary(class) and is_list(cards) do
    # TODO
    nil
  end

  def archetype(_cards, _), do: nil
end
